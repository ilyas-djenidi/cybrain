# Test File: Insecure AuthUserFile
# Storing credentials in files named "htpasswd" in default or guessable locations.

<Directory "/protected">
    AuthType Basic
    AuthName "Restricted"
    # VULNERABILITY: Common name/path might be targeted
    AuthUserFile /var/www/.htpasswd
    Require valid-user
</Directory>
